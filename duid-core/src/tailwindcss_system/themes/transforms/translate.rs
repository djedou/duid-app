use std::collections::HashMap;


pub(crate) fn translate() -> HashMap<String, String> {
    let mut translate = HashMap::new();
    let _ = translate.insert("translate-x-0".to_owned(), "transform: translateX(0px);".to_owned());
    let _ = translate.insert("translate-y-0".to_owned(), "transform: translateY(0px);".to_owned());
    let _ = translate.insert("translate-x-px".to_owned(), "transform: translateX(1px);".to_owned());
    let _ = translate.insert("translate-y-px".to_owned(), "transform: translateY(1px);".to_owned());
    let _ = translate.insert("translate-x-0.5".to_owned(), "transform: translateX(0.125rem);".to_owned());
    let _ = translate.insert("translate-y-0.5".to_owned(), "transform: translateY(0.125rem);".to_owned());
    let _ = translate.insert("translate-x-1".to_owned(), "transform: translateX(0.25rem);".to_owned());
    let _ = translate.insert("translate-y-1".to_owned(), "transform: translateY(0.25rem);".to_owned());
    let _ = translate.insert("translate-x-1.5".to_owned(), "transform: translateX(0.375rem);".to_owned());
    let _ = translate.insert("translate-y-1.5".to_owned(), "transform: translateY(0.375rem);".to_owned());
    let _ = translate.insert("translate-x-2".to_owned(), "transform: translateX(0.5rem);".to_owned());
    let _ = translate.insert("translate-y-2".to_owned(), "transform: translateY(0.5rem);".to_owned());
    let _ = translate.insert("translate-x-2.5".to_owned(), "transform: translateX(0.625rem);".to_owned());
    let _ = translate.insert("translate-y-2.5".to_owned(), "transform: translateY(0.625rem);".to_owned());
    let _ = translate.insert("translate-x-3".to_owned(), "transform: translateX(0.75rem);".to_owned());
    let _ = translate.insert("translate-y-3".to_owned(), "transform: translateY(0.75rem);".to_owned());
    let _ = translate.insert("translate-x-3.5".to_owned(), "transform: translateX(0.875rem);".to_owned());
    let _ = translate.insert("translate-y-3.5".to_owned(), "transform: translateY(0.875rem);".to_owned());
    let _ = translate.insert("translate-x-4".to_owned(), "transform: translateX(1rem);".to_owned());
    let _ = translate.insert("translate-y-4".to_owned(), "transform: translateY(1rem);".to_owned());
    let _ = translate.insert("translate-x-5".to_owned(), "transform: translateX(1.25rem);".to_owned());
    let _ = translate.insert("translate-y-5".to_owned(), "transform: translateY(1.25rem);".to_owned());
    let _ = translate.insert("translate-x-6".to_owned(), "transform: translateX(1.5rem);".to_owned());
    let _ = translate.insert("translate-y-6".to_owned(), "transform: translateY(1.5rem);".to_owned());
    let _ = translate.insert("translate-x-7".to_owned(), "transform: translateX(1.75rem);".to_owned());
    let _ = translate.insert("translate-y-7".to_owned(), "transform: translateY(1.75rem);".to_owned());
    let _ = translate.insert("translate-x-8".to_owned(), "transform: translateX(2rem);".to_owned());
    let _ = translate.insert("translate-y-8".to_owned(), "transform: translateY(2rem);".to_owned());
    let _ = translate.insert("translate-x-9".to_owned(), "transform: translateX(2.25rem);".to_owned());
    let _ = translate.insert("translate-y-9".to_owned(), "transform: translateY(2.25rem);".to_owned());
    let _ = translate.insert("translate-x-10".to_owned(), "transform: translateX(2.5rem);".to_owned());
    let _ = translate.insert("translate-y-10".to_owned(), "transform: translateY(2.5rem);".to_owned());
    let _ = translate.insert("translate-x-11".to_owned(), "transform: translateX(2.75rem);".to_owned());
    let _ = translate.insert("translate-y-11".to_owned(), "transform: translateY(2.75rem);".to_owned());
    let _ = translate.insert("translate-x-12".to_owned(), "transform: translateX(3rem);".to_owned());
    let _ = translate.insert("translate-y-12".to_owned(), "transform: translateY(3rem);".to_owned());
    let _ = translate.insert("translate-x-14".to_owned(), "transform: translateX(3.5rem);".to_owned());
    let _ = translate.insert("translate-y-14".to_owned(), "transform: translateY(3.5rem);".to_owned());
    let _ = translate.insert("translate-x-16".to_owned(), "transform: translateX(4rem);".to_owned());
    let _ = translate.insert("translate-y-16".to_owned(), "transform: translateY(4rem);".to_owned());
    let _ = translate.insert("translate-x-20".to_owned(), "transform: translateX(5rem);".to_owned());
    let _ = translate.insert("translate-y-20".to_owned(), "transform: translateY(5rem);".to_owned());
    let _ = translate.insert("translate-x-24".to_owned(), "transform: translateX(6rem);".to_owned());
    let _ = translate.insert("translate-y-24".to_owned(), "transform: translateY(6rem);".to_owned());
    let _ = translate.insert("translate-x-28".to_owned(), "transform: translateX(7rem);".to_owned());
    let _ = translate.insert("translate-y-28".to_owned(), "transform: translateY(7rem);".to_owned());
    let _ = translate.insert("translate-x-32".to_owned(), "transform: translateX(8rem);".to_owned());
    let _ = translate.insert("translate-y-32".to_owned(), "transform: translateY(8rem);".to_owned());
    let _ = translate.insert("translate-x-36".to_owned(), "transform: translateX(9rem);".to_owned());
    let _ = translate.insert("translate-y-36".to_owned(), "transform: translateY(9rem);".to_owned());
    let _ = translate.insert("translate-x-40".to_owned(), "transform: translateX(10rem);".to_owned());
    let _ = translate.insert("translate-y-40".to_owned(), "transform: translateY(10rem);".to_owned());
    let _ = translate.insert("translate-x-44".to_owned(), "transform: translateX(11rem);".to_owned());
    let _ = translate.insert("translate-y-44".to_owned(), "transform: translateY(11rem);".to_owned());
    let _ = translate.insert("translate-x-48".to_owned(), "transform: translateX(12rem);".to_owned());
    let _ = translate.insert("translate-y-48".to_owned(), "transform: translateY(12rem);".to_owned());
    let _ = translate.insert("translate-x-52".to_owned(), "transform: translateX(13rem);".to_owned());
    let _ = translate.insert("translate-y-52".to_owned(), "transform: translateY(13rem);".to_owned());
    let _ = translate.insert("translate-x-56".to_owned(), "transform: translateX(14rem);".to_owned());
    let _ = translate.insert("translate-y-56".to_owned(), "transform: translateY(14rem);".to_owned());
    let _ = translate.insert("translate-x-60".to_owned(), "transform: translateX(15rem);".to_owned());
    let _ = translate.insert("translate-y-60".to_owned(), "transform: translateY(15rem);".to_owned());
    let _ = translate.insert("translate-x-64".to_owned(), "transform: translateX(16rem);".to_owned());
    let _ = translate.insert("translate-y-64".to_owned(), "transform: translateY(16rem);".to_owned());
    let _ = translate.insert("translate-x-72".to_owned(), "transform: translateX(18rem);".to_owned());
    let _ = translate.insert("translate-y-72".to_owned(), "transform: translateY(18rem);".to_owned());
    let _ = translate.insert("translate-x-80".to_owned(), "transform: translateX(20rem);".to_owned());
    let _ = translate.insert("translate-y-80".to_owned(), "transform: translateY(20rem);".to_owned());
    let _ = translate.insert("translate-x-96".to_owned(), "transform: translateX(24rem);".to_owned());
    let _ = translate.insert("translate-y-96".to_owned(), "transform: translateY(24rem);".to_owned());
    let _ = translate.insert("translate-x-1/2".to_owned(), "transform: translateX(50%);".to_owned());
    let _ = translate.insert("translate-x-1/3".to_owned(), "transform: translateX(33.333333%);".to_owned());
    let _ = translate.insert("translate-x-2/3".to_owned(), "transform: translateX(66.666667%);".to_owned());
    let _ = translate.insert("translate-x-1/4".to_owned(), "transform: translateX(25%);".to_owned());
    let _ = translate.insert("translate-x-2/4".to_owned(), "transform: translateX(50%);".to_owned());
    let _ = translate.insert("translate-x-3/4".to_owned(), "transform: translateX(75%);".to_owned());
    let _ = translate.insert("translate-x-full".to_owned(), "transform: translateX(100%);".to_owned());
    let _ = translate.insert("translate-y-1/2".to_owned(), "transform: translateY(50%);".to_owned());
    let _ = translate.insert("translate-y-1/3".to_owned(), "transform: translateY(33.333333%);".to_owned());
    let _ = translate.insert("translate-y-2/3".to_owned(), "transform: translateY(66.666667%);".to_owned());
    let _ = translate.insert("translate-y-1/4".to_owned(), "transform: translateY(25%);".to_owned());
    let _ = translate.insert("translate-y-2/4".to_owned(), "transform: translateY(50%);".to_owned());
    let _ = translate.insert("translate-y-3/4".to_owned(), "transform: translateY(75%);".to_owned());
    let _ = translate.insert("translate-y-full".to_owned(), "transform: translateY(100%);".to_owned());

    translate
}

/*
*/