use std::collections::HashMap;


pub(crate) fn scale() -> HashMap<String, String> {
    let mut scale = HashMap::new();
    let _ = scale.insert("scale-0".to_owned(), "transform: scale(0);".to_owned());
    let _ = scale.insert("scale-x-0".to_owned(), "transform: scaleX(0);".to_owned());
    let _ = scale.insert("scale-y-0".to_owned(), "transform: scaleY(0);".to_owned());
    let _ = scale.insert("scale-50".to_owned(), "transform: scale(.5);".to_owned());
    let _ = scale.insert("scale-x-50".to_owned(), "transform: scaleX(.5);".to_owned());
    let _ = scale.insert("scale-y-50".to_owned(), "transform: scaleY(.5);".to_owned());
    let _ = scale.insert("scale-75".to_owned(), "transform: scale(.75);".to_owned());
    let _ = scale.insert("scale-x-75".to_owned(), "transform: scaleX(.75);".to_owned());
    let _ = scale.insert("scale-y-75".to_owned(), "transform: scaleY(.75);".to_owned());
    let _ = scale.insert("scale-90".to_owned(), "transform: scale(.9);".to_owned());
    let _ = scale.insert("scale-x-90".to_owned(), "transform: scaleX(.9);".to_owned());
    let _ = scale.insert("scale-y-90".to_owned(), "transform: scaleY(.9);".to_owned());
    let _ = scale.insert("scale-95".to_owned(), "transform: scale(.95);".to_owned());
    let _ = scale.insert("scale-x-95".to_owned(), "transform: scaleX(.95);".to_owned());
    let _ = scale.insert("scale-y-95".to_owned(), "transform: scaleY(.95);".to_owned());
    let _ = scale.insert("scale-100".to_owned(), "transform: scale(1);".to_owned());
    let _ = scale.insert("scale-x-100".to_owned(), "transform: scaleX(1);".to_owned());
    let _ = scale.insert("scale-y-100".to_owned(), "transform: scaleY(1);".to_owned());
    let _ = scale.insert("scale-105".to_owned(), "transform: scale(1.05);".to_owned());
    let _ = scale.insert("scale-x-105".to_owned(), "transform: scaleX(1.05);".to_owned());
    let _ = scale.insert("scale-y-105".to_owned(), "transform: scaleY(1.05);".to_owned());
    let _ = scale.insert("scale-110".to_owned(), "transform: scale(1.1);".to_owned());
    let _ = scale.insert("scale-x-110".to_owned(), "transform: scaleX(1.1);".to_owned());
    let _ = scale.insert("scale-y-110".to_owned(), "transform: scaleY(1.1);".to_owned());
    let _ = scale.insert("scale-125".to_owned(), "transform: scale(1.25);".to_owned());
    let _ = scale.insert("scale-x-125".to_owned(), "transform: scaleX(1.25);".to_owned());
    let _ = scale.insert("scale-y-125".to_owned(), "transform: scaleY(1.25);".to_owned());
    let _ = scale.insert("scale-150".to_owned(), "transform: scale(1.5);".to_owned());
    let _ = scale.insert("scale-x-150".to_owned(), "transform: scaleX(1.5);".to_owned());
    let _ = scale.insert("scale-y-150".to_owned(), "transform: scaleY(1.5);".to_owned());
    scale
}


/*
*/