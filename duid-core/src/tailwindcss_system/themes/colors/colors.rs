use std::collections::HashMap;


pub fn colors_styles() -> HashMap<String, String> {
    let mut styles = HashMap::new();
    let _ = styles.insert("color".to_owned(), "color".to_owned());
        
    styles
}
